# memory-footprint: план исследования и решения

**Трек закрыт 23.08.2026.** Все четыре направления пройдены: одно дало результат (утечка
diff-моделей исправлена), три закрыты измерениями. Итог — в [`result.md`](result.md).

Транзиентный спутник трека. Стабильных контрактов трек пока не меняет, поэтому `spec.md` не заводим —
он появится, если решим менять публичный контракт (кандидат: экспорт `disposeConsole()`).

Как ведём трек:

- **гипотеза → метод → числа** пишутся в [`research.md`](research.md), каждое направление своим разделом;
- **что выбрали и почему** — в журнал решений внизу этого файла;
- **итог для человека** — в [`result.md`](result.md);
- **текст для автора** — в [`issue.md`](issue.md).

Правило, выведенное на своей шкуре (см. решение от 22.08 про Enter-стресс): полевой замер
засчитывается только с **нормировкой на счётчик** (правки, вызовы подсказок, обращения за
метаданными), иначе Working Set скачет на ±150 МБ сам по себе и любая версия «подтверждается».

## 1. Направления

Порядок — по величине рычага. Каждое направление закрывается либо числом, либо явным «не подтвердилось».

### Этап 1. Возврат памяти при закрытии формы — ЗАКРЫТ, отрицательный (23.08.2026)

Проверяли, можно ли снести контекст поля самим, раз платформа отдаёт его не полностью.
Числа — в [`research.md` §2.0.2](research.md).

- Все пять способов сноса (`innerHTML`, `location.replace` двумя путями, обнуление реквизита
  `HTML`, вызов JS-teardown) движок поля **принимает без исключения** — проверено логом, а не
  молчаливой `Попыткой`.
- Памяти это не возвращает: осело 462–497 МБ над базой, ровно как и без сноса.
- Контрольная сборка-пустышка (та же обёртка и форма, в макете страница в десять строк) за
  5 циклов «открыл — закрыл» набрала **2.9 МБ на цикл** против **81 МБ** у консоли. Платформа
  и обёртка почти ничего не держат — держит содержимое поля.
- Первая инициализация консоли стоит клиенту **300–400 МБ безвозвратно**, каждая следующая
  добавляет затухающий остаток (36 МБ на циклах 2–5, 16 МБ на 6–8).

**Следствие для трека:** возвращать нечего. Рычаг ровно один — снижать то, что один экземпляр
выделяет. Этапы 2 и 3 становятся основными; `disposeConsole()` (этап 4) теряет смысл как
средство возврата памяти и остаётся только вопросом предсказуемости.

### Этап 2. Из чего состоит экземпляр — ЗАКРЫТ раскладкой (23.08.2026)

Полевая раскладка по четырём сборкам ([`research.md` §2.0.3](research.md)): бандл Monaco до
создания редактора — **246.8 МБ (61 %)**, создание редактора и наша обвязка — 88.4 МБ (22 %),
словари языка — 56.1 МБ (14 %), обёртка и WebView платформы — 14.5 МБ (4 %).

Ключевое: **память не пропорциональна объёму кода**. Словари — 4.7 МБ кода и 56 МБ памяти,
первые 4.9 МБ Monaco — 261 МБ. Платим за исполнение верхнего уровня модулей Monaco, а не за
объём JS.

Отсюда закрыты как рычаги:

- **Обрезка контрибов Monaco** — 2.1 % бандла (225 КБ). Попутно найдено, что
  `basic-languages/xml/xml.js` тянет весь редактор со всеми контрибами и обнуляет наш узкий entry
  `edcore.main`; вынос грамматики XML к себе это чинит, но выигрыш всё равно единицы мегабайт.
- **Уборка текста бандла из DOM** — 10.92 МБ текста скриптов освобождают ~3.5 МБ RSS.

Остаётся то, на что мы действительно влияем: 56 МБ словарей (этап 3) и 88 МБ обвязки, плюс
интеграционный рычаг B1 — не плодить экземпляры.

### Этап 3. Ленивые словари (A2) — ЗАКРЫТ, потолок слишком низкий (23.08.2026)

Потолок слоя словарей — 56.1 МБ, 14 % экземпляра. Замер самого приёма ([`research.md` §2.0.4](research.md))
показал, что взять из этих 14 % почти нечего:

- **Ленивый `JSON.parse` — около нуля.** Кириллица хранится по два байта на символ: 2.00 МБ данных
  дают 4 МБ строки, столько же, сколько весит готовый объект. Неразобранная строка стоит как литерал.
- **Сжатое хранение (gzip+base64) — примерно вдвое**, до ~30 МБ в поле, но ровно до первого
  обращения. Для консоли, где подсказки и есть основной сценарий, это выигрыш на минуту.
- **Отделить описания от структуры не поможет:** текстовые поля — лишь 24.5 % словаря, остальное
  имена, сигнатуры, параметры, типы.

Цена любого из вариантов — переделка горячего пути подсказок. Соотношение не сходится.

Попутно найдено (гигиена, не рычаг): `bsl_helper.js` статически импортирует `availabilityToArray`
из `./bslGlobals`, из-за чего словарь материализуется жадно и динамический `import()` в `editor.js`
ничего не откладывает. Если когда-нибудь вернёмся к теме — начинать с выноса хелперов в отдельный
модуль.

### Этап 4. `disposeConsole()` на `window` (A5) — потерял смысл как рычаг памяти

Задумывался как способ дать 1С свернуть редактор перед закрытием формы. Этап 1 показал, что
возврата памяти нет в принципе — ни своими руками, ни ожиданием. Остаётся только как вопрос
предсказуемости состояния, и в этом качестве в трек памяти не входит.

- **Меняет публичный контракт** → если браться, то отдельной спекой и с согласованием.

## 2. Открытые вопросы

- Отправляем ли фикс утечки автору и когда — решение отложено (22.08). До решения живёт на
  `local/webpack`. Отдельным маленьким PR может пойти находка про `xml.contribution`: она чинит
  узкий entry `edcore.main`, который сейчас обнуляется (выигрыш по памяти мал, но сборка честнее).
- Автозагрузка синтакс-помощника (+763 МБ на экземпляр) включена в нашей форме по умолчанию.
  Менять ли значение по умолчанию у себя и предлагать ли автору — отдельный разговор, в `issue.md`
  намеренно не вошло.
- Нужен ли `memstat` как постоянный инструмент под флагом сборки. Пока собирается временным патчем
  поверх `src/editor.js`, в репозиторий не вносился.

## 3. Решения

| Дата | Решение | Причина |
| --- | --- | --- |
| 2026-08-22 | Справку (HBK) исключить из области оптимизации | В сценарии PrintWizard файл `.hbk` не подаётся; без файла подсистема стоит 1–2 МБ. Замеры оставлены справочно |
| 2026-08-22 | Мерить в поле, а не только в Chrome | Утечка diff-моделей в Chrome почти не видна (модели шарят строки, GC работает): 3.5 МБ на 120 срабатываний против 4.1 МБ на одну правку в поле |
| 2026-08-22 | Стресс «зажать Enter» признан негодным | Дебаунс 50 мс схлопывает автоповтор в считанные вызовы: 20 секунд дали −27 МБ, и это едва не увело в вывод «утечки нет». Нагрузку строить с паузами |
| 2026-08-22 | Версия «виновата обвязка метаданных» снята | Счётчики показали: 7 обращений, 65 тыс. символов от 1С, `bslMetadata` +56 КБ. Сотен мегабайт там нет |
| 2026-08-22 | A/B делать двумя сборками с одинаковыми счётчиками | Одиночные замеры Working Set скачут на ±150 МБ; только нормировка на `EVENT_CONTENT_CHANGED` дала однозначный результат |
| 2026-08-22 | Утечку diff-моделей чинить переиспользованием модели, а не только `dispose()` прежней | `dispose()` убирает удержание, но оставляет churn: копия текста на каждую правку. Переиспользование снимает и то и другое |
| 2026-08-23 | Трек закрыт: в коде редактора рычагов не осталось | 61 % экземпляра — загрузка бандла Monaco до создания редактора; дальше только вмешательство в сам Monaco, что выходит за рамки консоли |
| 2026-08-23 | Ленивые словари закрыты как рычаг | Потолок слоя 14 %, из них ленивый разбор берёт ~0 (кириллица: строка стоит как объект), сжатое хранение — до первого обращения. Цена — горячий путь подсказок |
| 2026-08-23 | Обрезка контрибов Monaco закрыта как рычаг | Работающая обрезка дала 2.1 % бандла; при этом память не пропорциональна объёму кода — словари это 4.7 МБ кода и 56 МБ памяти против 4.9 МБ Monaco и 261 МБ |
| 2026-08-23 | Грамматику XML держать у себя, а не через `xml.contribution` | Штатный модуль тянет весь редактор со всеми контрибами и обнуляет узкий entry `edcore.main`. Гигиена сборки, не рычаг памяти |
| 2026-08-23 | Направление «снести контекст поля перед закрытием» закрыто | Все пять способов сноса движок принимает, памяти это не возвращает: осело столько же, сколько без сноса |
| 2026-08-23 | Контрольная сборка-пустышка обязательна перед выводом «виновата платформа» | Пустышка в той же обёртке даёт 2.9 МБ за цикл против 81 у консоли — сняло версию «платформа не сносит WebView» за один короткий прогон |
| 2026-08-23 | Приоритет сместить с возврата памяти на снижение пика | Возврата нет в принципе; при этом первая инициализация стоит 300–400 МБ, и это единственное, на что мы влияем |
| 2026-08-22 | Наши доработки увести на `local/webpack`, `webpack` держать зеркалом upstream | Чтобы ветка под PR отпочковывалась от чистого upstream и дифф был узким |
